# Torch is installed separately in the Dockerfile from the CPU wheel index to
# keep the image small (no CUDA). Everything else the app needs at runtime:
chess==1.11.2
Flask==3.1.3
waitress==3.0.2
huggingface_hub>=0.23
numpy
